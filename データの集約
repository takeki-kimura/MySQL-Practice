# 集約関数による値の集計

# 集約関数とは

  ・SQLでテーブルの値を集計するために使う
  ・関数(function)とは、様々な計算をパッケージ化したもの

# 主な集約関数

  ・sum(expr)・・・合計値
  ・avg(expr)・・・平均値
  ・min(expr)・・・最小値
  ・max(expr)・・・最大値
  ・count(expr)・・行数

  ・expr・・・expression,  日本語訳：式
    exprの部分は引数(ひきすう,parameter)と呼ばれ、呼び出しごとに異なる値を関数に渡すために使用される